spring.application.name=dscatalog

server.port=8080

spring.datasource.url=jdbc:postgresql://localhost:5432/ds-catalog
spring.datasource.username=postgres
spring.datasource.password=123


#logging.level.org.springframework.security=DEBUG

#spring.jpa.properties.hibernate.jdbc.lob.non-contextual-creation=true
#spring.jpa.properties.hibernate.format_sql=true
#spring.jpa.show-sql=true

#api.security.token.secret=${JWT_SECRET:my-secret-key}

#spring.output.ansi.enabled = Always
#spring.profiles.active=prod

#logging.level.com.ead=TRACE
#logging.level.root=INFO
#logging.level.org.springframework.web = DEBUG
#logging.level.org.hibernate = INFO


spring.jpa.hibernate.ddl-auto=update

spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

security.client-id=${CLIENT_ID:myclientid}
security.client-secret=${CLIENT_SECRET:myclientsecret}

security.jwt.duration=${JWT_DURATION:86400}

cors.origins=${CORS_ORIGINS:http://localhost:3000,http://localhost:5173, http://localhost:3000}


spring.mail.host=${EMAIL_HOST:smtp.gmail.com}
spring.mail.port=${EMAIL_PORT:587}
spring.mail.username=${EMAIL_USERNAME:test@gmail.com}
spring.mail.password=${EMAIL_PASSWORD:123456}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

email.password-recover.token.minutes=${PASSWORD_RECOVER_TOKEN_MINUTES:30}
email.password-recover.uri=${PASSWORD_RECOVER_URI:http://localhost:8080/recover-password/}
